#include<stdio.h>
int main()
{
	float b,h,area;
	printf("Enter the value of base: ");
	scanf("%f",&b);
	printf("Enter the value of height:");
	scanf("%f",&h);
	area=(b*h)/2;
	printf("Area of Triangle is:%f",area);
	return 0;
}

